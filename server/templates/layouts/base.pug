include ../mixins/flash-messages.pug

doctype html
html
    head
        meta(charset="utf-8")
        meta(name="robots" content="noindex")
        meta(name="viewport", content="initial-scale=1.0,user-scalable=no,maximum-scale=1,width=device-width")

        title= title || 'Split Seven'
        link(rel="shortcut icon", href="/favicon.ico", type="image/x-icon")
        link(href="https://fonts.googleapis.com/css?family=Cabin:400,400i,700,700i" rel="stylesheet")
        link(href=`/assets/styles/site.css?v=${version}`, rel="stylesheet")

        if env === 'production'
            script.
                (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
                (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
                m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
                })(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

                ga('create', 'UA-86339108-2', 'auto');
                ga('send', 'pageview');

        block css

        block head

    body

        block header
            include ../partials/header.pug

        //- +flash-messages(messages)

        block content

        block js
            script(src=`/assets/scripts/vendor.js?v=${version}`)
            script(src=`/assets/scripts/main.js?v=${version}`)

